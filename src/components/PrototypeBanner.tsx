export function PrototypeBanner() {
  return (
    <div className="w-full border-b border-black bg-gray-100 px-4 py-2 text-center text-xs text-gray-700">
      NON-FUNCTIONAL PROTOTYPE — a phone UI mocked up for the web. Nothing
      records, researches, or listens. Every screen and transcript is
      hardcoded.
    </div>
  );
}
